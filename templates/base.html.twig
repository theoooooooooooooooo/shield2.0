<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Super Hero Agency{% endblock %}</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
        <link href="https://fonts.googleapis.com/css2?family=Bangers&display=swap" rel="stylesheet">
        {% block stylesheets %}
        <style>
            /* Setup Flexbox for full height layout */
            body {
                height: 100%;
                font-family: 'Bangers', cursive;
                background: linear-gradient(45deg, #1c1c1c, #333) no-repeat;
                color: #fff;
                display: flex;
                flex-direction: column;
                min-height: 100vh;
                margin: 0; /* Reset margin to ensure proper alignment */
            }

            /* Main content takes up remaining space */
            main {
                flex: 1; /* Fills the available space */
                display: flex;
                flex-direction: column;
            }

            .navbar {
                background-color: #000;
            }

            .navbar-brand, .nav-link {
                color: #f8e71c !important;
            }

            .nav-link:hover {
                color: #50e3c2 !important;
            }

            .hero-banner {
                background: url('/images/superhero-bg.jpg') no-repeat center center / cover;
                height: 50vh;
                display: flex;
                justify-content: center;
                align-items: center;
                color: #fff;
                text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.8);
            }

            .btn-hero {
                background-color: #f8e71c;
                color: #000;
                font-size: 1.2rem;
                border-radius: 5px;
                box-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
            }

            .btn-hero:hover {
                background-color: #50e3c2;
                color: #fff;
            }

            footer {
                background: #111;
                color: #777;
                padding: 20px 0;
                text-align: center;
            }
            .form-container {
                display: flex;
                justify-content: center;
                align-items: center;
                min-height: 80vh;
            }

            .form-card {
                background-color: #1c1c1c;
                border-radius: 10px;
                padding: 30px;
                width: 100%;
                max-width: 600px;
                box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
                color: white;
            }

            .form-card h2 {
                text-align: center;
                color: #f8e71c;
            }

            .form-card .form-group {
                margin-bottom: 15px;
            }

            .btn-primary {
                background-color: #f8e71c;
                border: none;
                color: black;
                width: 100%;
            }

            .btn-primary:hover {
                background-color: #50e3c2;
                color: white;
            }

            .btn-secondary {
                width: 100%;
            }
            .btn-danger {
                width: 100%;
            }
            .badge {
        font-size: 0.9rem;
        padding: 0.5rem;
        border-radius: 5px;
        margin-right: 0.5rem;
    }

    .badge-disponible {
        background-color: #28a745;
        color: white;
    }
    .badge-non-disponible {
        background-color: #dc3545;
        color: white;
    }

    .btn-details {
        background-color: #f8e71c;
        color: #000;
        border: none;
        font-weight: bold;
        transition: 0.3s ease-in-out;
    }
    .btn-details:hover {
        background-color: #50e3c2;
        color: #fff;
    }
        .checkbox-list {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
    }

    .checkbox-item {
        display: inline-block;
        margin-right: 15px;
    }
        </style>
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand" href="/">🦸 Super Agency</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_SuperHero') }}">Heroes</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_Pouvoir') }}">Pouvoir</a></li>
                        <li class="nav-item"><a class="nav-link" href="/mission">Missions</a></li>
                        <li class="nav-item"><a class="nav-link" href="/team">Team</a></li>
                    </ul>
                </div>
            </div>
        </nav>

        <main>
            {% block body %}
            {% endblock %}
        </main>

        <footer>
            <p>&copy; {{ "now"|date("Y") }} Super Hero Agency - Saving the world, one mission at a time!</p>
        </footer>

        <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
