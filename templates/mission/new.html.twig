{% extends 'base.html.twig' %}

{% block title %}Nouvelle Mission{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="container form-container">
        <div class="form-card">
            <h2>Nouvelle mission</h2>
            {{ include('mission/_form.html.twig', {'button_label': 'Update'}) }}
            <div class="mt-4">
                <a href="{{ path('app_mission_index') }}" class="btn btn-secondary">Retour</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
